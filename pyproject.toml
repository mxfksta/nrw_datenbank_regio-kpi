[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
addopts = "-q"
filterwarnings = [
    # openpyxl warnt bei Workbooks ohne Default-Styles (unsere synthetischen Fixtures)
    "ignore::UserWarning:openpyxl",
]
