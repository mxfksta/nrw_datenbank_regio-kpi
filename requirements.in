# Laufzeit-Abhängigkeiten (kompiliert nach requirements.txt via:
#   uv pip compile requirements.in -o requirements.txt
# alternativ: pip-compile requirements.in)
google-cloud-bigquery>=3.25
openpyxl>=3.1
pandas>=2.2
pdfplumber>=0.11
pydantic>=2.7
PyYAML>=6.0
requests>=2.32
tenacity>=8.3
